// src/h.ts
import {
  createElement as h
} from "@kitajs/html";
var createElement = (name, attrs, ...children) => {
  const { $elysia, ...attr } = attrs ?? {};
  return h(name, attr, ...children);
};
export {
  createElement
};
